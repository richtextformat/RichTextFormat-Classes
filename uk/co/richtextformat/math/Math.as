package uk.co.richtextformat.math
{
	public class Math
	{
		public static function pythagoras (opposite:Number, adjacent:Number):Number
		{
			return Math.sqrt( (opposite * opposite) + (adjacent * adjacent) );
		}
	}
}
